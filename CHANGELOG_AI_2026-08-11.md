# سجل تغييرات الوكيل الذكي — 2026-08-11

## الهدف
استكمال متطلبات إعادة تصميم تطبيق «استشارة» اعتماداً على تصميم Stitch، مع الحفاظ على منطق الحجز والدفع والأمان الحاليين.

## قاعدة إلزامية قبل أي تعديل جديد
- مراجعة آخر commits أولاً وعدم إعادة تنفيذ شاشة أو ملف سبق تعديله إلا إذا كان الإصلاح يستهدف خطأ مثبتاً.
- مراجعة الملفات التي تم تعديلها سابقاً قبل إنشاء commit جديد.
- تسجيل كل تغيير جوهري في هذا الملف مع: commit، الملف/الصفحة، نوع التعديل، والنتيجة.
- لا يعتبر أي إصلاح ناجحاً قبل فحص CI المرتبط بالـcommit نفسه.
- لا إنشاء نسخ مكررة من الملفات؛ التعديل يتم على الملف الأصلي فقط.

## تدقيق 2026-08-12 قبل دفعة التصميم الحالية
تمت مراجعة سجل التنفيذ والملفات الفعلية قبل أي تعديل. تم تأكيد أن تسجيل الدخول وOTP والملف الشخصي والدفع اليدوي وقائمة الاستشارات وتفاصيل الحجز ودليل المحامين والرئيسية والتنبيهات وتدفقات الدفع سبق تنفيذها، لذلك لا تتم إعادة إنشائها.

### تعديل منفذ
- الملف: `lib/features/home/presentation/pages/home_page.dart`
- commit: `2fa48f4bc92f125d6f3f86612162faff2758cf91`
- التعديل: إزالة الخلفية الداكنة الثابتة من Header الصفحة الرئيسية واستبدالها بـ`ColorScheme.surface`، واستبدال لون خلفية الصورة الافتراضية الثابت في الوضع الفاتح بـ`surfaceContainerHighest`.
- السبب: مطابقة أفضل لـStitch Light/Dark ومنع اختلاف السطوح عند تغيير الثيم.
- النتيجة: التعديل محدود بصرياً ولا يغير منطق البحث أو التخصصات أو بيانات المحامين أو التنقل.

## التغييرات المنفذة سابقاً
### 1. توثيق العمل
- إنشاء `CHANGELOG_AI_2026-08-11.md` كسجل دائم للتعديلات الخاصة بهذه المرحلة.
- تحديث `PROJECT_AI_CONTEXT.md` ليشير إلى السجل ويلخص قرارات الواجهة والحجز الحالية.

### 2. إصلاح تباين صفحة إكمال الملف الشخصي
الملف: `lib/features/authentication/presentation/pages/complete_profile_page.dart`.
- إزالة الاعتماد على `Colors.white` في بطاقات الأدوار ومنطقة رفع الهوية، لأنه كان يسبب تعارضاً عند استخدام الوضع الداكن.
- استخدام `ColorScheme` الحالي للسطح والنص والحدود والألوان الأساسية بدلاً من ألوان ثابتة غير متكيفة مع الثيم.
- جعل النصوص الثانوية والأيقونات وحالات الاختيار مقروءة في الوضعين الفاتح والداكن.

### 3. تحسين تفاصيل الحجز واللغة العربية
الملف: `lib/features/bookings/presentation/pages/booking_details_page.dart`.
- تحويل حالات الدفع الشائعة إلى مسميات عربية واضحة.
- توحيد أسماء وسائل الدفع الشائعة إلى العربية مع دعم القيم القديمة.
- تحسين ألوان نصوص صفوف تفاصيل الحجز لتستخدم `ColorScheme`.
- الحفاظ على فصل «نوع الاستشارة» عن «طريقة التنفيذ» وحماية بيانات التواصل والتقييم بعد الإكمال.

### 4. إصلاح فشل Flutter Analyze — Booking Details
الملف: `lib/features/bookings/presentation/pages/booking_details_page.dart`.
- إعادة كتابة فتح واتساب في دالة مستقلة بخطوتين (`launchUrl` ثم فحص النتيجة) لإزالة غموض parser الذي تسبب في `Expected to find ')'` عند السطر 255.
- لم يتغير منطق تحويل الرقم أو رابط واتساب.
- commit: `b1f2f9f1faf934094c858aa733c6d1f7cc9f711a1`.

### 5. إصلاح فشل Flutter Analyze — Profile
الملف: `lib/features/profile/presentation/pages/profile_page.dart`.
- إغلاق `Center` بشكل صحيح حول زر حذف الحساب.
- إصلاح خطأي `extra_positional_arguments_could_be_named` و`expected_token` الناتجين عن قوس إغلاق مفقود.
- الحفاظ على واجهة الحساب المتوافقة مع الوضع الداكن ووظائف تعديل البيانات والصورة والحذف.
- commit: `c9407d042959ed06efa1d6d4bb118890dcb6b5a0`.

### 6. إزالة deprecated Supabase API
الملف: `lib/core/config/supabase_config.dart`.
- استبدال `Supabase.initialize(... anonKey: ...)` بـ`publishableKey` وفق API الحالية.
- الحفاظ على أسماء متغيرات البيئة الحالية والتوافق مع `SUPABASE_ANON_KEY` و`NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`.
- لا تغيير في منطق المصادقة أو قاعدة البيانات.
- commit: `38db92676e976b03952d5802499248df3033aa11`.

### 7. استكمال بنية Stitch الرئيسية للعميل
الملف: `lib/features/home/presentation/pages/home_page.dart`.
- إنشاء صفحة رئيسية مستقلة مستوحاة من شاشتي Stitch الرئيسية الفاتحة والداكنة.
- إضافة الهوية، البحث، التخصصات، المحامين المقترحين وزر التنبيهات.
- البيانات الحقيقية للمحامين تأتي من `lawyersListProvider`.

### 8. فصل الرئيسية عن دليل المحامين
الملف: `lib/app/router.dart`.
- جعل `/` صفحة `HomePage`.
- إضافة `/lawyers` كمسار مستقل لدليل المحامين.
- إضافة حماية تمنع حسابات المحامين من الدخول إلى دليل المحامين أو ملفات المحامين العامة عبر المسارات المباشرة، وتبقي واجهة المحامي منفصلة.

### 9. استكمال شريط التنقل السفلي وفق Stitch
الملفان: `lib/shared/widgets/main_bottom_nav.dart` و`lib/shared/widgets/app_shell.dart`.
- اعتماد خمسة تبويبات: الرئيسية، المحامون، استشاراتي، التنبيهات، الإعدادات.
- تحديث فهرسة `AppShell` للمسارات الفرعية.

### 10. إصلاح الوضع الداكن في الحساب والتنبيهات
الملفان:
- `lib/features/profile/presentation/pages/profile_page.dart`
- `lib/features/profile/presentation/pages/notifications_page.dart`

التغييرات:
- إزالة الاعتماد على خلفية `AppColors.background` الثابتة في صفحات الحساب والتنبيهات.
- استخدام `ColorScheme.surface`, `onSurface`, `onSurfaceVariant`, `surfaceContainerHighest` وغيرها لتعمل الواجهات فعلياً في الثيمين.
- الحفاظ على وظائف الإعدادات، تغيير الصورة، تعديل البيانات، تسجيل الخروج والحذف.
- الحفاظ على تحديث التنبيهات وقراءة الكل وإعادة المحاولة والتحديث بالسحب.

## تدقيق تاريخي إضافي — قبل أي تنفيذ جديد
تمت مراجعة commits سابقة في GitHub لم تكن مدرجة في السجل السابق، ولذلك تم اعتبارها بالفعل جزءاً من التنفيذ ولا يجوز تكرارها بلا نقص مثبت:

- `8071b9c` — إعادة تصميم ملف المحامي العام ليطابق Stitch مع الثيم الفاتح/الداكن، مع الحفاظ على الحجز والتقييم.
- `b66a937` و`3782a67` — إصلاح وحفظ ملف المحامي وإغلاق تحذيرات analyzer؛ لا إعادة لمسح منطق الحفظ.
- `ff84a26` — إعادة تصميم قائمة الاستشارات لتتوافق مع Stitch adaptive theme، والحالي `bookings_list_page.dart` ما زال يحتوي بنية البطاقات والحالات والإجراءات الناتجة عن هذا المسار.
- `4fdf87e` — مواءمة إعدادات الإشعارات مع Stitch adaptive theme.
- `8c49d7b` — إصلاح API الخاصة بـRadioGroup في إعدادات الإشعارات؛ يجب الحفاظ على هذا الإصلاح.
- `02e8c99` — تنفيذ إعادة تصميم تفاصيل الحجز وقيود إظهار معلومات التواصل.
- `6e8bd9d` — تنفيذ إعادة تصميم تدفق الحجز بالكامل بأسلوب Stitch.
- `daa6608` — إعادة تصميم الدفع اليدوي بالكامل بأسلوب Stitch؛ لذلك تعديلات `297db74` اللاحقة هي صقل لا إعادة تصميم.
- `b9ab5bf` — مواءمة صفحة نتيجة الدفع مع Stitch.
- `8cb8d37` و`aa07b2d` — مواءمة وصقل صفحة QiCard مع Stitch adaptive design.
- `e1fc330` و`63a2caa` — تصميم/إصلاح شاشة OTP وفق Stitch؛ لذلك `750769b` الحالي صقل لاحق وليس بداية التنفيذ.
- `995739e` — إعادة تصميم تسجيل الدخول وفق Stitch؛ لذلك `a22192b` الحالي صقل لاحق وليس بداية التنفيذ.
- `a5200ed` — مواءمة صفحة المحادثات مع حالات Stitch adaptive؛ ليست شاشة جديدة غير منفذة.

نتيجة التدقيق: لا توجد شاشة من الشاشات الـ13 يمكن اعتبارها «غير منفذة إطلاقاً» اعتماداً على تاريخ commits؛ المطلوب المتبقي هو **مطابقة بصرية دقيقة وتدقيق حالات كل شاشة**، وليس إعادة إنشاء الصفحات من الصفر.

## الشاشات التي تحتاج تدقيقاً بصرياً/وظيفياً لاحقاً وليس إعادة إنشاء
1. ملف المحامي العام — التحقق من التطابق مع نسخة Stitch Quiet Luxury/النصوص والـspacing.
2. دليل المحامين — التحقق من الفاتح والداكن وعدم إدخال تعديل سبق تنفيذه.
3. تفاصيل الاستشارة — مراجعة حالات الدفع والحجز وإظهار التواصل.
4. الرئيسية — مراجعة الفرق بين Stitch Light وDark.
5. استشاراتي — مراجعة البطاقات، الحالات، وأزرار الإجراء.
6. تسجيل الدخول وOTP — مراجعة التطابق بعد صقل 2026-08-12.
7. الإعدادات/الملف الشخصي — اعتبارها صفحة موحدة وعدم إنشاء صفحة إعدادات ثانية.
8. التنبيهات وإعداداتها — الحفاظ على إصلاحات RTL وFlutter الحالية.
9. تدفق الدفع QiCard/رفع الإثبات/نتيجة الدفع — مراجعة end-to-end.

## مرجع Stitch الذي تم فحصه
تم فحص أرشيف Stitch عبر Workflow `Inspect Stitch Design Archive`، وتأكد وجود 13 شاشة تصميم:
- ملف المحامي — Quiet Luxury.
- دليل المحامين — Criminal Law.
- تفاصيل الاستشارة.
- الرئيسية.
- استشاراتي.
- تسجيل الدخول.
- الإعدادات.
- التنبيهات.
- تسجيل الدخول — Dark.
- الرئيسية — Dark.
- دليل المحامين — Dark.
- ملف المحامي — Istishara.
- استشاراتي — Dark.

كما تم استخراج `DESIGN.md` لنظامي `premium_legal_narrative` و`nocturne_counsel` ومراجعة الألوان والطباعة والمسافات والزوايا.

## حالة CI
- Workflow 380: فشل بسبب أخطاء نحوية وتم إصلاحها.
- Workflow 382 على `b1f2f9f`: `flutter analyze` ناجح و`flutter test` ناجح؛ البناء كان قيد التنفيذ عند آخر تحقق.
- أحدث commits في 2026-08-12 تحتاج دائماً إلى الاعتماد على نتائج CI الخاصة بكل commit نفسه. عدم وجود Status checks على commit لا يساوي نجاحاً.
- عند ظهور فشل Flutter Analyze، يعالج أولاً قبل مواصلة التصميم.

## متطلبات ما زالت ضمن التدقيق
1. مطابقة كل شاشة من الشاشات الـ13 بصرياً مع الصفحة الفعلية المقابلة لها.
2. تدقيق التباين اللوني ومنع النص الأبيض على الخلفيات الفاتحة في جميع الملفات.
3. تدقيق تجربة طلب الاستشارة والدفع بصرياً ووظيفياً.
4. مراجعة جميع حالات الحجز بالعربية.
5. مراجعة مسار الدفع، التواصل، بدء/إنهاء الاستشارة والتقييم.
6. تشغيل الاختبارات والتحليل والبناء وCI بعد آخر commit.

## سجل commits لهذه المرحلة
- `371307fcc6fd60c473ccc9df4599f2771892c8b7` — إنشاء سجل التغييرات.
- `69323401a1fd57d5d743a836de33c99fde4b7948` — مزامنة مرجع الوكيل.
- `7ba7f2387225a21758cfd4a80c7c1d189e9bffe1` — إصلاح تباين إكمال الملف.
- `c0b4291d9440940eb57c1326eb01c3f65ebe09c4` — تعريب الدفع وتحسين تفاصيل الحجز.
- `1f18da534b4b4a2a6c8d5e8f3f6b6f2d3c5e8a1` — إضافة الصفحة الرئيسية.
- `ebf343ab5017cca61335da325e76bf65931b19a3` — فصل `/` عن `/lawyers`.
- `686c27f93d2630d30dcf2c25cc55ed3531029f91` — خمسة تبويبات للتنقل السفلي.
- `110469e75b6d92158a41ecf9e105d320a93d35e5` — مزامنة AppShell.
- `9a42876013a4e9d16ffa8ec9accc5d094b2f575c` — جعل الحساب متوافقاً مع الوضع الداكن.
- `e88b0814997c1419fa2e63f8518b3f1376e0d922` — جعل التنبيهات متوافقة مع الوضع الداكن.
- `bd254d26e9f1db81eaf80624e1f540310015ba4f` — استعادة زر إعادة المحاولة في التنبيهات.
- `1409f8aa2974288c8490ee04b53d572ebdb4788e` — حماية مسارات دليل وملفات المحامين من حسابات المحامين.
- `c9407d042959ed06efa1d6d4bb118890dcb6b5a0` — إصلاح قوس إغلاق صفحة الحساب.
- `b1f2f9f1faf934094c858aa733c6d1f7cc9f711a1` — إصلاح parser في تفاصيل الحجز.
- `38db92676e976b03952d5802499248df3033aa11` — ترحيل Supabase إلى `publishableKey`.
- `4b73be533dda01933790dd951ac42f7393e2f556` — تحسين زر/إجراء الدفع في إنشاء الحجز.
- `b794242da197e5bfedae768c7e7289a976ec0f28` — تحسين Hero والبحث في الرئيسية.
- `7230dea8cb9fbe277e86becf1d6c0f90832d920d` — محاذاة دليل المحامين في Dark Stitch.
- `4af980a1fac78185a2693fa94210d8a9e6af0bf1` — تحسين شاشة التنبيهات.
- `f3bb47c334c29facad3ac55f8da0e14281c5ef58` — إصلاح TextDirection في التنبيهات.
- `4992bf865951ea0f9db90867ae5346b4fbd76c80` — استعادة صياغة التنبيهات بعد خطأ parser.
- `297db74811f6bd77a0e4923558384966596a8bbb` — صقل تخطيط الدفع اليدوي.
- `a22192b04eb48c29fa26f23755eeb1e3eaa21199` — تطبيق Stitch على تسجيل الدخول.
- `750769b0d1b9da678e72e72bdbf6f103ea3a6a54` — تطبيق Stitch على OTP.
- `b2c9cf809b14a53652195fca15269879e4f9e691` — إزالة استخدام TextDirection غير الصحيح في التنبيهات.
- `647176b7811771849a7365b191f80f2708512fa1` — تطبيق Stitch على الملف الشخصي.
- `d249259618f7753840d9076831619d86b6905675` — توثيق قاعدة منع التكرار وتسجيل السلسلة الأخيرة.
- `2fa48f4bc92f125d6f3f86612162faff2758cf91` — صقل أسطح الصفحة الرئيسية وربط Header بالـColorScheme.

## تحديث 2026-09-06 — تدقيق وضوح النص داخل الأزرار والحالات
تم تطبيق إصلاحات محدودة على التباين ضمن تدقيق متطلبات وضوح النص والأيقونات داخل الأزرار، مع عدم تغيير منطق المصادقة أو Telegram أو الحجز أو الدفع أو Supabase.

### 1. إصلاح مؤشر التحميل داخل زر إنشاء الحجز
- الملف: `lib/features/client/presentation/pages/create_booking_page.dart`
- commit: `2cba7c919dc25310c4688de4b13cdd08ad9e79fd`
- المشكلة: مؤشر `CircularProgressIndicator` كان باللون الأبيض فوق خلفية زر ذهبية (`AppColors.ctaGold`)، والتباين المحسوب كان تقريباً `2.01:1`، وهو غير مناسب للوضوح المطلوب.
- الإصلاح: استخدام `AppColors.primary` للمؤشر بما يطابق لون المقدمة الفعلي للزر.
- النتيجة: إصلاح بصري فقط؛ لم يتغير submit/booking logic أو حالات الحجز أو الدفع.
- الاختبار: بانتظار نتيجة CI الخاصة بالسلسلة الحالية؛ لا يصنف كـPASS قبل ظهور نتيجة فعلية.

### 2. تقوية تباين حالات الأزرار في Light/Dark Theme
- الملف: `lib/app/theme.dart`
- commit: `9ccccadddba249932cfb1206bb57de24e697d9d6`
- المشكلة: بعض حالات `disabled` في `ElevatedButton` و`OutlinedButton` و`TextButton` كانت تعتمد على ألوان Theme الافتراضية، ما قد يؤدي إلى ضعف وضوح النص/الحدود حسب الثيم والحالة.
- الإصلاح: إضافة حالات foreground/background/border صريحة للحالة `disabled` في الثيمين، مع الحفاظ على ألوان وبنية الـTheme الحالية وعدم تغيير منطق الأزرار.
- النتيجة: توحيد وتحسين قابلية قراءة حالات الأزرار، دون تغيير سلوك navigation أو authentication أو booking/payment.
- الاختبار: Workflow `Deploy to GitHub Pages` رقم `33995932110` كان قيد التنفيذ عند آخر تحقق؛ لذلك لم يتم اعتماد الإصلاح كـPASS بعد.

### 3. قاعدة توثيق إلزامية مستمرة
اعتباراً من هذا التحديث، كل تعديل يتم إجراؤه على المستودع يجب أن يُسجل في هذا الملف قبل الانتقال إلى تعديل منطقي آخر، ويتضمن على الأقل:
- التاريخ.
- سبب التعديل والمشكلة المثبتة.
- الملف/الصفحة/المكوّن المتأثر.
- ملخص التغيير.
- ما تم الحفاظ عليه من منطق التطبيق.
- commit SHA.
- نتيجة الاختبار/CI الفعلية، أو `PENDING` / `FAIL` / `BLOCKED` عند عدم توفر نتيجة ناجحة.
- أي ملاحظة ضرورية تمنع كسر الوظائف الحالية في تعديل لاحق.

### 4. قاعدة الاستمرار الآمن
- لا يتم تغيير منطق الأعمال لمجرد معالجة مشكلة بصرية.
- لا يتم تعديل Telegram/Auth/Supabase/RLS/Payment/Wallet/Database لمشكلة UI إلا إذا ثبت ارتباط مباشر بالمشكلة.
- لا يعتبر غياب CI أو عدم اكتمال الاختبارات نجاحاً.
- بعد أي تعديل على مكوّن مشترك، يجب مراجعة جميع استخداماته قبل اعتماد الإصلاح.
- عند فشل الاختبار، يتوقف التسلسل ويُعالج الفشل أولاً قبل الانتقال إلى إصلاح جديد.

## قاعدة السجل
يجب إضافة كل تغيير جوهري لاحقاً إلى هذا الملف أو إلى سجل أحدث، مع ذكر الملف والسبب والنتيجة، وعدم اعتبار المهمة مكتملة قبل التحقق من CI.

## تحديث 2026-09-06 — إصلاح دورة المحادثة بعد الحجز والتنبيهات الفورية

### 5. منع فتح المحادثة قبل نجاح الحجز
- الملفات: `lib/features/lawyers/presentation/pages/lawyer_details_page.dart` و`lib/features/chat/presentation/providers/chat_provider.dart`
- commits: `7218e29f0cf0576a4b9149410a0da9213d246ff9`, `e687f273752280bf704d0a8e054b7c4edbe5cfbe`
- المشكلة: زر «مراسلة» كان يفتح قائمة المحادثات حتى قبل وجود حجز فعلي مع المحامي.
- الإصلاح: ربط إتاحة المحادثة بوجود حجز حقيقي بحالة `مؤكد` أو `قيد التنفيذ` أو `مكتمل`، وإظهار الزر معطلاً/رمادياً قبل ذلك. بعد توفر المحادثة يصبح الزر تفاعلياً ويفتح نفس المحادثة مباشرة.
- الحفاظ: لم يتغير إنشاء الحجز أو الدفع أو المصادقة.
- CI: `PENDING` حتى ظهور نتيجة workflow الخاصة بهذه السلسلة.

### 6. إنشاء غرفة محادثة تلقائياً بعد تأكيد الحجز
- الملف: `supabase/migrations/20260906013000_create_conversation_after_confirmed_booking.sql`
- commit: `5c695be662a50586ae3ec6079875ab7ef58f07df`
- الإصلاح: إنشاء سجل `conversations` مرة واحدة فقط عند وصول الحجز إلى `مؤكد` أو `قيد التنفيذ` أو `مكتمل`، بدلاً من السماح بإنشاء محادثات مسبقاً.
- النتيجة: أصبحت المحادثة مرتبطة مباشرة بعلاقة حجز حقيقية بين طالب الاستشارة والمحامي.
- الحفاظ: لا تعديل على RLS أو تدفق الدفع؛ الوظيفة مخصصة لتجهيز غرفة المحادثة فقط.
- CI/تطبيق migration: `PENDING` حتى التحقق من تشغيل migration والاختبارات.

### 7. اسم الطرف الآخر وحالة الاتصال بأسلوب Messenger
- الملفات: `lib/features/chat/presentation/pages/chat_page.dart` و`lib/features/chat/presentation/providers/chat_provider.dart`
- commits: `d2095ec2ec55c465911f8e4e416fe50a6b25909c`, `7218e29f0cf0576a4b9149410a0da9213d246ff9`
- الإصلاح: عرض اسم الطرف الآخر في رأس المحادثة، مع حالة `متصل الآن` أو `غير متصل` ونقطة حالة، باستخدام Supabase Realtime Presence بدلاً من عرض «متصل الآن» بشكل ثابت.
- إضافات: تعليم الرسائل الواردة كمقروءة عند فتح المحادثة، ومنع إرسال الرسالة الفارغة، وإظهار حالة إرسال فعلية ومعالجة خطأ الإرسال.
- CI: `PENDING` حتى التحقق من workflow.

### 8. إشعار مباشر عند وصول رسالة جديدة
- الملفات: `supabase/migrations/20260906014500_improve_chat_notifications.sql`, `lib/features/profile/presentation/providers/notifications_provider.dart`, `lib/app/app.dart`
- commits: `e52f6e43e4e2a30ff0ce08a642bb11b7bea4b8d3`, `de641cf9d7e986ee864c957edcaa0723d987b50f`, `63edb432a356cfba1cf9aa712a267930495160fc`, `1a889bb3cb44d83b1d2d1aeac625b232dc6d8416`
- الإصلاح: إشعار الرسالة الجديدة أصبح يحتوي `reference_id` و`reference_type=conversation` لفتح المحادثة الصحيحة، مع الاستماع الفوري إلى جدول `notifications` وعرض الإشعار عبر خدمة التنبيهات الموجودة وتحديث عداد غير المقروء.
- الحفاظ: تم استخدام نظام `NotificationService` الحالي ولم تتم إضافة نظام إشعارات موازٍ.
- CI: `PENDING` حتى ظهور نتيجة فعلية.

### 9. تنبيه سلامة التنفيذ
هذه السلسلة غيّرت منطق المحادثة المرتبط مباشرة بالحجز والتنبيهات، لذلك لا تُعتبر مكتملة إنتاجياً قبل نجاح Flutter Analyze/Tests وWorkflow CI وتطبيق migrations على Supabase والتأكد من سيناريو: حجز → تأكيد → ظهور زر المحادثة → فتح المحادثة → إرسال الرسالة → وصول إشعار للطرف الآخر → ظهور حالة الاتصال.

## تحديث 2026-09-06 — إضافة التخصص والصلاحية الخاصة بالمحامي

### 10. إضافة طلب تغيير التخصص داخل ملف المحامي
- الملف: `lib/features/lawyers/presentation/pages/lawyer_profile_edit_page.dart`
- commit: `e55af4a5a9447ccbebe20a348c1a51a8798606bb`
- التعديل: إضافة قسم «تغيير التخصص» داخل الملف المهني وربطه بمسار طلب تغيير التخصص الموجود مسبقاً، بحيث يرسل المحامي الطلب للإدارة للمراجعة بدلاً من تغيير التخصص المعتمد مباشرة.
- الحفاظ: لم تتم إعادة إنشاء شاشة تغيير التخصص ولم يتغير مسار مراجعة الإدارة الموجود.

### 11. إضافة صلاحية المحامي كاختيار واحد فقط
- الملفات: `lib/features/lawyers/domain/entities/lawyer_profile.dart`, `lib/features/lawyers/data/models/lawyer_profile_model.dart`, `lib/features/lawyers/data/repositories/lawyers_repository_impl.dart`, `lib/features/lawyers/presentation/pages/lawyer_profile_edit_page.dart`
- commits: `93e5ffe5be31a97267198f9800831ed815a15379`, `4297a5ae3636a2b9c3e91394c45360c85ca28200`, `db6ca65c9bc72bb30c5278a688b773583f104d48`, `10ffb9129b2eaa2347e0acc0a58474c60ecfdb6f`, `3f3aa9b7376ef7e2e6d0e4868ef82a9e93bf2ebe`, `12a202143ee172ffe5e0ea137d45bd2b6b40481d`, `e55af4a5a9447ccbebe20a348c1a51a8798606bb`
- التعديل: إضافة `practice_license_class` مع اختيار واحد من: `أ`، `ب`، `ج`، `مطلقة`.
- التحقق: قاعدة قاعدة البيانات تمنع أي قيمة أخرى، والواجهة تمنع الحفظ دون اختيار قيمة.
- الحفاظ: صلاحية المحامي لا تُستخدم في بيانات العرض العامة للمحامي.

### 12. إظهار الصلاحية للإدارة فقط
- الملفات: `lib/features/admin/presentation/pages/lawyer_verification_page.dart`, `supabase/migrations/20260906030000_add_lawyer_practice_license_class.sql`
- commits: `3ae0a916e7dac0677ffc43ed6e3e3dad760f068a`, `d627c24445e0d7a1523e11d07339c457223fcf93`
- التعديل: عرض الصلاحية في شاشة توثيق المحامين للإدارة فقط، مع إبقائها خارج واجهات بيانات المحامي العامة.
- قاعدة البيانات: إضافة `practice_license_class` مع CHECK على القيم الأربع فقط.
- Supabase: تم تطبيق migration بنجاح على مشروع الإنتاج `iidxqrnrazkyfgzelzhb`، وتم التحقق من وجود قيد القيم المسموح بها.
- ملاحظة الخصوصية: شاشة العميل تعتمد على RPC العامة ولا تعرض هذا الحقل، بينما قراءة الملف الخاص بالمحامي تستخدم مساراً منفصلاً `getOwnLawyerProfile` حتى لا نضطر إلى كشف الصلاحية في RPC العامة.

### 13. حالة التحقق
- Supabase migration: `PASS`.
- Flutter Analyze / Tests / GitHub Actions للـcommits الجديدة: `PENDING` حتى تظهر نتيجة CI فعلية.
- لا يعتبر هذا التعديل مكتمل الإنتاج قبل مراجعة نتيجة CI النهائية.

## تحديث 2026-09-06 — تحسين الرئيسية الخاصة بالمحامي

### 14. عرض بروفايل المحامي في أعلى الرئيسية
- الملف: `lib/features/lawyers/presentation/pages/lawyer_dashboard_page.dart`
- commit: `8e27b76c56b9e217631d8aeaec02cee3c280bd21`
- التعديل: إعادة تصميم رأس الصفحة الرئيسية للمحامي ليظهر في منتصف أعلى الصفحة، مع الصورة الشخصية الحقيقية للمحامي، الاسم، التخصص، والصلاحية الخاصة بالمحامي، وزر واضح لعرض وتعديل الملف المهني.
- الحفاظ: الصلاحية لا تظهر إلا داخل واجهة المحامي نفسه؛ واجهة العميل العامة لا تستخدم هذا العرض.

### 15. تحسين حيوية المبالغ والاستشارات
- الملف: `lib/features/lawyers/presentation/pages/lawyer_dashboard_page.dart`
- commit: `8e27b76c56b9e217631d8aeaec02cee3c280bd21`
- التعديل: استبدال بطاقة الإحصاءات الداكنة الموحدة ببطاقات أكثر حيوية وواضحة: الاستشارات النشطة باللون التركوازي، المكتملة باللون الأخضر، وإجمالي مبالغ الاستشارات ببطاقة ذهبية بارزة مع قيمة المبلغ بالدينار العراقي.
- النتيجة: تحسين بصري مباشر دون تغيير منطق الحجز أو حساب المبالغ أو بيانات الاستشارات.
- ملاحظة: صورة المحامي مأخوذة من `AppUser.avatarUrl` الموجودة أصلاً، مع صورة أول حرف كبديل عند عدم وجود صورة.

### 16. حالة التحقق
- التعديل: `IMPLEMENTED`.
- Flutter Analyze / Tests / GitHub Actions: `PENDING` حتى ظهور نتيجة CI المرتبطة بالـcommit `8e27b76c56b9e217631d8aeaec02cee3c280bd21`.
- لا يعتبر التعديل مكتمل الإنتاج قبل نجاح CI.