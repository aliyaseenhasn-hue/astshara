# استشارة — سياق المشروع للوكيل الذكي

> هذا الملف هو **مرجع تشغيل موحد للوكيل الذكي** عند العمل على المشروع. يجب قراءته قبل أي تعديل، ثم الرجوع إلى الكود وقاعدة Supabase للتحقق من أن الواقع الحالي يطابق التوثيق. مصدر الحقيقة للكود هو آخر commit فعلي على `main`.

## 1. هوية المشروع
- الاسم: استشارة (LawConnect / astshara)
- المستودع: `aliyaseenhasn-hue/astshara`
- الفرع الرئيسي: `main`
- المنصة: Flutter، Android/iOS/Web، مع Supabase كـ backend.
- المعمارية: Clean Architecture + Feature-First.
- إدارة الحالة: Riverpod.
- التنقل: GoRouter / ShellRoute.
- قاعدة البيانات: Supabase PostgreSQL.
- Realtime: Supabase Realtime.
- الملفات الحساسة: Supabase Private Storage + Signed URLs.
- الإشعارات: نظام إشعارات حقيقي للمحامي وطالب الخدمة.
- الهوية البصرية الحالية: ذهبي + سماوي مع الثيم الحالي للمشروع؛ لا تستبدل الهوية بدون سبب.
- الواجهة RTL واللغة التشغيلية للحالات والرسائل للمستخدم هي العربية.

## 2. قاعدة العمل الإلزامية
1. لا تبدأ من الصفر ولا تعِد تنفيذ إصلاح سابق.
2. افحص `main` وآخر commit قبل التعديل.
3. افحص الملفات المرتبطة بالمشكلة ثم Supabase/RLS/RPC/Edge Functions عند الحاجة.
4. لا تخمّن مخطط قاعدة البيانات أو أسماء الأعمدة.
5. الحلول المؤقتة وMock ممنوعة لحل مشاكل حقيقية.
6. إذا كان الإصلاح يحتاج قاعدة بيانات، أنشئ Migration حقيقية وطبّقها.
7. إذا كان يحتاج Edge Function، عدّلها وانشرها.
8. بعد كل تعديل: راجع الكود، شغّل الفحص المناسب، اقرأ الأخطاء وأصلحها، ثم أعد الفحص.
9. لا تعتبر المهمة منتهية بسبب نجاح Dart فقط؛ يجب التحقق من CI والنشر عندما تكون الأدوات متاحة.
10. لا تحذف ميزة قائمة إلا إذا كان ذلك ضرورياً، مع الحفاظ على وظيفتها الأصلية.

## 3. بنية الميزات المهمة
- `lib/core`: الإعدادات والخدمات المشتركة.
- `lib/features/authentication`: المصادقة والملفات الشخصية الأولية.
- `lib/features/lawyers`: المحامون وملفاتهم والتخصصات.
- `lib/features/bookings`: الحجز، حالات الحجز، بدء/إنهاء الاستشارة، الدفع اليدوي.
- `lib/features/payments`: الدفع ورفع الإيصالات.
- `lib/features/chat`: المحادثات.
- `lib/features/reviews`: تقييم المحامي بعد اكتمال الاستشارة.
- `lib/features/profile`: الإعدادات والإشعارات والملف الشخصي.
- `lib/features/admin`: إدارة المحامين وطلبات تغيير التخصص.
- `lib/shared/widgets/app_shell.dart`: الحاوية الرئيسية والتنقل السفلي.
- `lib/shared/widgets/main_bottom_nav.dart`: الشريط السفلي الوحيد.
- `PROJECT_MASTER_DOCUMENTATION.md`: التوثيق التاريخي/التقني السابق.
- `PROJECT_AI_CONTEXT.md`: هذا المرجع التشغيلي الموحد، ويجب تحديثه مع كل تعديل جوهري.

## 4. الشريط السفلي والتنقل
- الشريط السفلي يجب أن يظهر مرة واحدة فقط من `AppShell`.
- `MainBottomNav` يحتوي: الرئيسية، الحجوزات، المحادثات، الإعدادات.
- لا تضف BottomNavigationBar داخل الصفحات الفرعية.
- صفحات الإعدادات والإشعارات وطرق الدفع والمساعدة يجب أن تعود إلى تبويب الإعدادات.
- عند الضغط على تبويب محدد داخل صفحة فرعية، يجب إعادة المستخدم إلى الصفحة الأساسية لذلك التبويب.

## 5. منطق الحجز الحالي
الحالات الأساسية العربية:
- `قيد انتظار الدفع`
- `قيد معالجة الدفع`
- `قيد مراجعة المحامي`
- `بانتظار التأكيد`
- `مؤكد`
- `قيد التنفيذ`
- `مكتمل`
- `ملغي`
- `مسترد`
- `بانتظار الاسترداد`
- `بانتظار مراجعة عدم الحضور`

لا يجوز أن تتعارض الحالات مع الدفع أو أوقات الجلسة.

### شروط بدء الاستشارة
لا يجوز الانتقال إلى `قيد التنفيذ` إلا إذا:
- الحجز مؤكد.
- الدفع مكتمل وفق نوع الدفع.
- الاستشارة ليست ملغاة/مستردة/مرفوضة.
- الوقت يسمح بالبدء وفق سياسة الموعد.
- لم تبدأ الاستشارة مسبقاً.

عند البدء:
- يسجل `started_at` فعلياً.
- تتغير الحالة إلى `قيد التنفيذ`.
- يمنع البدء الثاني عبر تحقق قاعدة البيانات/RPC، وليس الواجهة فقط.

عند الإنهاء:
- يجب وجود `started_at`.
- تسجل `completed_at`.
- تتحول الحالة إلى `مكتمل`.
- يتم تحديث عداد الاستشارات المكتملة للمحامي عبر المنطق الموجود في قاعدة البيانات.
- يصبح تقييم المحامي متاحاً لطالب الخدمة.

## 6. أنواع الاستشارة
الأنواع المستخدمة في الواجهة/البيانات تشمل:
- `نصية`
- `صوتية`
- `فيديو`
- `في المكتب`

### نصية/صوتية/فيديو عن بُعد
حالياً لا توجد غرفة فيديو داخلية WebRTC/Agora/Jitsi مؤكدة في الكود. لذلك لا تنشئ غرفة فيديو وهمية.
- التواصل الحالي للاستشارات عن بُعد يعتمد على بيانات الطرف الآخر المرتبطة بالحجز.
- لا تستخدم رقم واتساب ثابتاً أو تجريبياً.
- يتم تطبيع الرقم قبل إنشاء رابط `wa.me`.

### في المكتب
- لا تعاملها كواتساب أو فيديو.
- يجب أن يكون تنفيذها حضورياً.
- الدفع اليدوي المكتبي يبقى معلقاً حتى يسجل المحامي المبلغ المستلم.
- لا تسمح ببدء الجلسة قبل اكتمال الدفع اليدوي.
- لا تعرض أرقام واتساب كوسيلة بدء للاستشارة المكتبية.

## 7. الدفع اليدوي للمكتب
الحالة `بانتظار التأكيد` تعني أن حجز المكتب يحتاج تسجيل المبلغ المستلم من المحامي.
الشروط المطلوبة قبل السماح بالبدء:
- الحجز مؤكد من جهة الحجز.
- وجود سجل دفع مكتمل/مستلم.
- وجود `manual_received_at`.
- مطابقة المبلغ المستلم مع رسوم الاستشارة.

لا تعتمد الواجهة وحدها على هذه الشروط؛ يجب أن يفرضها RPC/قاعدة البيانات.

## 8. RPC وأمن قاعدة البيانات
من RPCs المهمة:
- `change_booking_status(p_booking_id uuid, p_new_status text)` — يجب أن تكون محمية ولا يسمح لـ anon بتنفيذها.
- `get_booking_participant_contact_info(p_booking_id uuid)` — يجب أن تكون محمية وتعيد فقط بيانات الطرف المرتبط بالحجز بعد تحقق الصلاحيات والحالة.

حالة الأمان التي تم التحقق منها أثناء جلسة العمل:
- `anon_execute = false`
- `authenticated_execute = true`
للـRPCs المذكورة أعلاه.

## 9. معلومات التواصل
- الاسم الكامل لطالب الخدمة يجب أن يظهر للمحامي إذا كانت البيانات متوفرة.
- لا تستخدم `عميل` أو `عميل جديد` كبديل إذا كان الاسم الحقيقي متوفراً.
- لا تعرض معلومات حساسة قبل تحقق شروط الحجز والدفع.
- لا تعرض WhatsApp للمكتب.

## 10. التقييم
- التقييم متاح بعد `مكتمل`.
- طالب الخدمة هو من يقيم المحامي.
- التقييمات يمكن أن تظهر للمستخدمين وفق منطق التطبيق.
- البيانات الداخلية مثل أرقام الدفع أو بيانات حساسة لا تظهر للمستخدمين بشكل غير ضروري.

## 11. ميزات يجب الحفاظ عليها أثناء الإصلاح
- تسجيل الخروج.
- حذف الحساب.
- إعدادات الإشعارات.
- الوضع الداكن والفاتح.
- الإشعارات الحقيقية.
- المحادثات.
- حذف/أرشفة المواعيد السابقة حسب السياسة الحالية.
- طلب تغيير تخصص المحامي مع صورة هوية النقابة وموافقة الإدارة.
- رفع صور الإنجازات.
- الدفع ورفع الإيصالات.
- الدفع اليدوي للمكتب.
- الاسم الكامل لطالب الخدمة.
- إعدادات طرق الدفع ومركز المساعدة.

## 12. آخر إصلاحات جلسة العمل في أغسطس 2026
### إصلاح تدفق بدء الاستشارة والدفع اليدوي
تمت إضافة Migration لتشديد شروط بدء الاستشارة في Supabase. المنطق يمنع بدء الحجز المكتبي إذا لم يكن الدفع اليدوي مسجلاً ومطابقاً للرسوم، ويمنع إنهاء جلسة لم تسجل `started_at`.

تم أيضاً ضبط صلاحيات RPCs بحيث لا يستطيع `anon` استدعاء عمليات تغيير الحالة أو معلومات التواصل.

### حماية معلومات التواصل للمكتب
تم تعديل منطق معلومات التواصل بحيث لا يعيد أرقام WhatsApp للحجز المكتبي.

### CI
تم تعديل workflow بحيث يعمل عند `push` إلى `main` بالإضافة إلى Pull Request، لأن تشغيل PR وحده لا يكفي لمراقبة آخر نسخة على الفرع الرئيسي.

## 13. ملفات مرتبطة مباشرة بتدفق الحجز
- `lib/features/bookings/presentation/pages/bookings_list_page.dart`
- `lib/features/bookings/presentation/pages/booking_details_page.dart`
- `lib/features/bookings/presentation/pages/manual_payment_page.dart`
- `lib/features/bookings/presentation/pages/manual_payment_required_page.dart`
- `lib/features/bookings/presentation/pages/create_booking_page.dart`
- `lib/features/bookings/presentation/providers/bookings_provider.dart`
- `lib/features/bookings/data/models/booking_model.dart`
- `lib/features/bookings/domain/entities/booking.dart`
- `lib/features/payments/presentation/providers/payments_provider.dart`
- `supabase/migrations/*booking*`
- `supabase/migrations/*consultation*`

## 14. حالة الواجهة الحالية التي تم فحصها
`AppShell` يحتوي `MainBottomNav` مرة واحدة فقط، و`MainBottomNav` نفسه يملك عناصر التنقل الأربعة ويستخدم GoRouter.

صفحة الحجوزات تستخدم زر `تفاصيل وبدء الاستشارة` عند `مؤكد`، وتستخدم زر إنهاء/الاستشارة الجارية عند `قيد التنفيذ`.

صفحة تفاصيل الحجز:
- المحامي يستطيع بدء الاستشارة عند `مؤكد` ضمن نافذة الوقت.
- النافذة الحالية تبدأ قبل الموعد بـ5 دقائق وتنتهي بعد مدة الباقة.
- عند `قيد التنفيذ` يظهر زر إنهاء للمحامي.
- طالب الخدمة يستطيع فتح WhatsApp بعد بدء الاستشارة للاستشارات عن بُعد.

### ملاحظة إصلاحية مهمة مفتوحة
يجب التأكد أن `booking_details_page.dart` لا ينفذ فتح WhatsApp عند بدء حجز `في المكتب`. السلوك الصحيح للمكتب هو بدء الجلسة وتسجيل وقت البدء دون فتح WhatsApp، مع رسالة/واجهة حضور مناسبة. هذا التحقق يجب أن يتم على الكود الحالي قبل أي تعديل جديد.

## 15. اختبارات القبول النهائية
لا تعتبر المهمة مكتملة حتى تتحقق قدر الإمكان من:
- `flutter analyze` بلا أخطاء.
- `flutter build web` ناجح.
- CI ناجح.
- لا أخطاء Dart compilation.
- Bottom navigation يظهر مرة واحدة.
- لا تكرار لأيقونة الجرس.
- أزرار Bottom navigation تعمل.
- الإعدادات تعمل.
- تسجيل الخروج وحذف الحساب موجودان ويعملان.
- بدء الاستشارة يحترم الدفع والحالة والوقت.
- بدء الاستشارة مرة واحدة فقط.
- `قيد التنفيذ` له `started_at`.
- `مكتمل` له `completed_at` وبدأت الجلسة مسبقاً.
- المكتب لا يفتح WhatsApp.
- الدفع اليدوي يمنع الميزات قبل تسجيل الاستلام.
- حالات الدفع والحجز متوافقة.
- لا توجد بيانات حساسة غير ضرورية في الواجهة.

## 16. سجل تغييرات الوكيل
يجب إضافة سجل جديد هنا عند كل تعديل مهم، بالتاريخ والسبب والملفات ونتيجة التحقق.

### 2026-08-10 — تشديد بدء الاستشارة
- السبب: منع بدء الاستشارة قبل اكتمال شروط الدفع والحجز.
- النطاق: Supabase booking lifecycle / manual office payment.
- النتيجة: فرض شروط الدفع اليدوي ووقت البدء/الإنهاء داخل قاعدة البيانات، وليس الواجهة فقط.
- الأمان: منع تنفيذ RPCs الحساسة من `anon`.
- التحقق: فحص وظائف RPC وحالة البيانات الحالية.

### 2026-08-10 — توثيق موحد للوكيل
- إنشاء هذا الملف ليكون سياقاً واحداً قابلاً للإرسال إلى أي وكيل ذكي.
- يجب تحديثه بعد كل إصلاح جوهري.
