# سجل مراجعة الدورة المالية — 2026-08-22

## المرجع
تمت متابعة العمل على الفرع `fix/financial-cycle-errors` وPR #17 بعد مراجعة المرجع الموحد للمشروع وسجل الإصلاحات المالية.

## ما تم اكتشافه بعد الإصلاح السابق
1. طلب السحب يقرّب المبلغ إلى منزلتين عشريتين عند إنشاء الطلب، لكن الإصلاح السابق كان يخصم من `available_balance` بالقيمة غير المقربة؛ هذا قد يسبب فرقاً محاسبياً.
2. بعض مسارات السحب كانت تستخدم `greatest(0, balance - amount)`، ما قد يخفي نقصاً حقيقياً في الرصيد بدلاً من إيقاف المعاملة.
3. كان `ensure_financial_accounting_for_payment` يستخدم قيمة عمولة افتراضية `0` عند غياب إعداد العمولة؛ هذا غير آمن لأن الدفعات الجديدة يجب أن تستخدم إعداد المنصة الحالي بدلاً من إنشاء عملية بدون عمولة بسبب إعداد مفقود.
4. واجهة الإدارة كانت تسمح بعرض انتقال `تم التحويل` من حالة `pending_review` رغم أن RPC لا يسمح بهذا الانتقال؛ تم توحيد الواجهة مع دورة الحالات.
5. مرجع التحويل كان معروضاً كاختياري حتى في حالة `paid` رغم أن RPC يشترطه؛ تم جعل الواجهة واضحة ومتوافقة مع الخادم.

## التعديلات المنفذة
### Migration
الملف:
`supabase/migrations/20260822030000_harden_payout_balance_and_transitions.sql`

تم فيه:
- جعل إعداد العمولة إلزامياً وصالحاً قبل إنشاء السجل المالي.
- منع إنشاء دورة مالية جديدة بمبلغ غير صالح.
- الإبقاء على حساب `gross = commission + lawyer_net` في الخادم.
- منع تسوية أرباح المحامي إذا كان `pending_balance` أقل من المبلغ المراد تسويته.
- توحيد مبلغ طلب السحب إلى منزلتين عشريتين قبل الحجز والخصم.
- جعل حجز مبلغ السحب والـledger يستخدمان نفس المبلغ المقرب.
- منع إتمام السحب إذا كان الرصيد المحجوز أقل من مبلغ الطلب.
- منع إعادة مبلغ السحب عند `rejected/failed` إذا لم يكن الرصيد المحجوز كافياً.
- تشديد انتقالات طلب السحب إلى:
  `pending_review -> approved -> processing -> paid`
  مع السماح بالإغلاق بـ`rejected/failed` وفق الحالة المسموح بها.
- منع إغلاق طلب مسحوب سابقاً مرة ثانية.
- الإبقاء على `provider_reference` إلزامياً قبل `paid`.
- الإبقاء على سبب `rejected/failed` إلزامياً.

### Flutter
الملف:
`lib/features/admin/presentation/pages/financial_management_page.dart`

تم فيه:
- إزالة خيار `تم التحويل` من طلبات `pending_review`.
- جعل الواجهة تتبع الانتقالات الخادمية المتسلسلة.
- جعل مرجع التحويل مطلوباً بصرياً عند `paid`.
- منع إرسال تأكيد `paid` من الواجهة إذا كان المرجع فارغاً.
- جعل سبب الرفض/الفشل مطلوباً في الواجهة قبل RPC.

## التحقق
- آخر CI مكتمل قبل هذه الدفعة: Run #846، وكانت وظائف `analyze` و`build` ناجحة، و`deploy` متخطية لأنها كانت نتيجة PR وليست push إلى `main`.
- بعد آخر commit للواجهة بدأ Workflow جديد Run #848 وحالته عند لحظة التسجيل `pending`؛ لذلك لا تعتبر نتيجة CI لهذه الدفعة ناجحة بعد.
- لم يتم الادعاء بأن Migration طُبقت على Supabase الفعلي لأن معرّف مشروع Supabase لم يكن متاحاً في أدوات هذه الجلسة.

## قاعدة الاستمرار
لا تعتبر الدورة المالية مكتملة حتى يتم التحقق فعلياً من:
- الدفع 100,000 -> عمولة 10,000 -> صافي 90,000.
- رفض الصفر والسالب.
- Webhook مكرر دون Ledger/Financial duplicate.
- إكمال الاستشارة مرة واحدة فقط.
- طلبي سحب متزامنين لا ينتجان حجزين نشطين.
- رفض/فشل السحب يعيد المبلغ مرة واحدة.
- paid لا يخصم مرة ثانية.
- عدم إمكانية تعديل الرصيد أو الـledger من العميل/المحامي مباشرة.
- نجاح `flutter analyze`, `flutter test`, و`flutter build web` في CI بعد آخر commit.
