import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';
const SUPABASE_URL=Deno.env.get('SUPABASE_URL')!;const SERVICE_ROLE=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!;const ANON_KEY=Deno.env.get('SUPABASE_ANON_KEY')!;const BOT_TOKEN=Deno.env.get('TELEGRAM_BOT_TOKEN')!;const BOT_USERNAME=(Deno.env.get('TELEGRAM_BOT_USERNAME')||'Astshara_app_bot').replace(/^@/,'');const WEBHOOK_SECRET=Deno.env.get('TELEGRAM_WEBHOOK_SECRET')||'';
const admin=createClient(SUPABASE_URL,SERVICE_ROLE,{auth:{autoRefreshToken:false,persistSession:false}});const authClient=createClient(SUPABASE_URL,ANON_KEY,{auth:{autoRefreshToken:false,persistSession:false}});const cors={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization, x-client-info, apikey, content-type','Access-Control-Allow-Methods':'POST, OPTIONS','Content-Type':'application/json; charset=utf-8'};const json=(b:unknown,s=200)=>new Response(JSON.stringify(b),{status:s,headers:cors});
const normalizePhone=(v:string)=>{let p=String(v||'').trim().replace(/\s+/g,'').replace(/[^0-9+]/g,'');if(p.startsWith('+'))p=p.slice(1);if(p.startsWith('00'))p=p.slice(2);if(p.startsWith('0'))p=`964${p.slice(1)}`;if(!p.startsWith('964'))p=`964${p}`;return p};const phoneCandidates=(p:string)=>[p,`0${p.slice(3)}`,`+${p}`];const normalizeCode=(v:string)=>String(v||'').replace(/[٠-٩]/g,c=>String('٠١٢٣٤٥٦٧٨٩'.indexOf(c))).replace(/\s/g,'');const sha256=async(t:string)=>{const d=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(t));return [...new Uint8Array(d)].map(b=>b.toString(16).padStart(2,'0')).join('')};const randomCode=()=>String(Math.floor(100000+Math.random()*900000));const randomPassword=()=>crypto.randomUUID()+crypto.randomUUID();const syntheticEmail=(p:string)=>`telegram.${p}@login.astshara.app`;const LINKED='هذا الحساب مرتبط بحساب آخر في تطبيق استشارة.';
async function telegram(m:string,b:Record<string,unknown>){const r=await fetch(`https://api.telegram.org/bot${BOT_TOKEN}/${m}`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(b)});const d=await r.json();if(!d.ok)throw new Error(d.description||`Telegram ${m} failed`);return d.result}async function ensureWebhook(){if(WEBHOOK_SECRET)await telegram('setWebhook',{url:`${SUPABASE_URL}/functions/v1/telegram-auth-v2?webhook=1`,secret_token:WEBHOOK_SECRET,allowed_updates:['message']})}async function findProfile(p:string){const{data,error}=await admin.from('profiles').select('*').in('phone',phoneCandidates(p)).limit(1).maybeSingle();if(error)throw new Error(`تعذر التحقق من الرقم: ${error.message}`);return data}async function findTelegramProfile(id:number){const{data,error}=await admin.from('profiles').select('*').eq('telegram_user_id',id).limit(1).maybeSingle();if(error)throw new Error(`تعذر التحقق من حساب Telegram: ${error.message}`);return data}
async function start(phoneRaw:string,mode='login',fullName?:string,role?:string){const phone=normalizePhone(phoneRaw);if(!/^9647\d{9}$/.test(phone))throw new Error('رقم الهاتف العراقي غير صحيح');const signup=mode==='signup';if(signup){if(!fullName||fullName.trim().length<3)throw new Error('الاسم الكامل مطلوب');if(role!=='user'&&role!=='lawyer')throw new Error('نوع الحساب غير صحيح');if(await findProfile(phone))throw new Error('هذا الرقم مرتبط بحساب آخر. يرجى استخدام رقم هاتف آخر أو تسجيل الدخول بالحساب المرتبط به.')}await ensureWebhook();const token=crypto.randomUUID();await admin.from('telegram_login_requests').update({status:'expired'}).eq('phone',phone).in('status',['waiting','code_sent','telegram_verified']);const{error}=await admin.from('telegram_login_requests').insert({request_token:token,phone,status:'waiting',attempts:0,expires_at:new Date(Date.now()+600000).toISOString(),mode:signup?'signup':'login',full_name:signup?fullName!.trim():null,role:signup?role:null});if(error)throw new Error(`تعذر إنشاء طلب Telegram: ${error.message}`);return{ok:true,request_token:token,telegram_url:`https://t.me/${BOT_USERNAME}?start=${token}`}}
async function webhook(req:Request){if(WEBHOOK_SECRET&&req.headers.get('x-telegram-bot-api-secret-token')!==WEBHOOK_SECRET)return json({ok:false},401);const u=await req.json(),m=u?.message;if(!m?.chat?.id)return json({ok:true});const id=Number(m.chat.id),text=String(m.text||'').trim(),arg=text.match(/^\/start(?:\s+(.+))?$/i)?.[1]?.trim();if(!arg){await telegram('sendMessage',{chat_id:id,text:'مرحباً بك في بوت استشارة ⚖️\nاضغط «بدء» من رابط التسجيل في التطبيق.'});return json({ok:true})}const{data:r,error}=await admin.from('telegram_login_requests').select('*').eq('request_token',arg).maybeSingle();if(error||!r||new Date(r.expires_at).getTime()<=Date.now()||r.status==='expired'){await telegram('sendMessage',{chat_id:id,text:'انتهت صلاحية طلب التحقق. ارجع إلى تطبيق استشارة واطلب محاولة جديدة.'});return json({ok:true})}const existing=await findTelegramProfile(id);if(existing){if(r.mode==='login'){await admin.from('telegram_login_requests').update({telegram_user_id:id,status:'telegram_verified',verified_profile_id:existing.id}).eq('id',r.id);return json({ok:true})}await telegram('sendMessage',{chat_id:id,text:LINKED});return json({ok:true})}const code=randomCode(),hash=await sha256(`${code}:${arg}`);const{error:ue}=await admin.from('telegram_login_requests').update({telegram_user_id:id,telegram_username:m.from?.username??null,telegram_first_name:m.from?.first_name??null,telegram_last_name:m.from?.last_name??null,code_hash:hash,status:'code_sent',attempts:0}).eq('id',r.id);if(ue)throw new Error(ue.message);await telegram('sendMessage',{chat_id:id,text:`رمز التحقق الخاص بك في استشارة: ${code}\n\nالرمز صالح لمدة 10 دقائق.`});return json({ok:true})}
async function verify(token:string,rawCode?:string){const{data:r,error}=await admin.from('telegram_login_requests').select('*').eq('request_token',token).maybeSingle();if(error)throw new Error(error.message);if(!r)throw new Error('طلب التحقق غير موجود');if(new Date(r.expires_at).getTime()<=Date.now()){await admin.from('telegram_login_requests').update({status:'expired'}).eq('id',r.id);throw new Error('انتهت صلاحية رمز التحقق. اطلب رمزاً جديداً')}if(r.mode==='login'&&r.status==='telegram_verified'&&r.telegram_user_id){const p=await findTelegramProfile(Number(r.telegram_user_id));if(!p||!p.auth_id)throw new Error('تعذر استرجاع الحساب المرتبط');return await createSessionForProfile(r,p)}const code=normalizeCode(rawCode||'');if(!r.telegram_user_id||r.status!=='code_sent')throw new Error('افتح Telegram واضغط «بدء» أولاً');if(!/^\d{6}$/.test(code))throw new Error('رمز التحقق يجب أن يتكون من 6 أرقام');if((r.attempts??0)>=5)throw new Error('تم تجاوز عدد المحاولات المسموح بها. اطلب رمزاً جديداً');const expected=await sha256(`${code}:${token}`);if(expected!==r.code_hash){await admin.from('telegram_login_requests').update({attempts:(r.attempts??0)+1}).eq('id',r.id);throw new Error('رمز التحقق غير صحيح')}const phone=r.phone,mode=r.mode||'login',profile=await findProfile(phone),tp=await findTelegramProfile(Number(r.telegram_user_id));if(tp&&(!profile||tp.id!==profile.id))throw new Error(LINKED);if(mode==='signup'&&profile)throw new Error('هذا الرقم مرتبط بحساب آخر. يرجى استخدام رقم هاتف آخر أو تسجيل الدخول بالحساب المرتبط به.');if(mode==='login'&&!profile)throw new Error('لم يتم العثور على حساب مرتبط بهذا الرقم. يرجى إنشاء حساب أولاً.');let authUser:any;if(profile?.auth_id)authUser=(await admin.auth.admin.getUserById(profile.auth_id)).data.user;else{const email=syntheticEmail(phone),password=randomPassword(),role=r.role==='lawyer'?'lawyer':'user',fullName=String(r.full_name||'').trim();const{data:created,error:ce}=await admin.auth.admin.createUser({email,password,email_confirm:true,phone:`+${phone}`,phone_confirm:true,user_metadata:{full_name:fullName,role}});if(ce)throw new Error(/already|exists|registered|duplicate/i.test(ce.message)?'هذا الرقم مرتبط بحساب آخر. يرجى استخدام رقم هاتف آخر أو تسجيل الدخول بالحساب المرتبط به.':`تعذر إنشاء الحساب: ${ce.message}`);authUser=created.user;const{error:ie}=await admin.from('profiles').insert({auth_id:authUser.id,phone,email,full_name:fullName,role,telegram_user_id:r.telegram_user_id});if(ie){await admin.auth.admin.deleteUser(authUser.id);if(ie.code==='23505')throw new Error(LINKED);throw new Error(`تعذر إنشاء ملف المستخدم: ${ie.message}`)}}return await createSessionForProfile(r,profile||await findProfile(phone))}
async function createSessionForProfile(r:any,p:any){const user=(await admin.auth.admin.getUserById(p.auth_id)).data.user;if(!user)throw new Error('تعذر استرجاع الحساب');const email=user.email||syntheticEmail(r.phone),password=randomPassword();const{error:ue}=await admin.auth.admin.updateUserById(user.id,{password,user_metadata:{...(user.user_metadata||{}),full_name:p.full_name,role:p.role}});if(ue)throw new Error(`تعذر تجهيز جلسة الدخول: ${ue.message}`);const{data:login,error:le}=await authClient.auth.signInWithPassword({email,password});if(le||!login.session)throw new Error(`تعذر إنشاء جلسة الدخول: ${le?.message||'الجلسة غير متاحة'}`);await admin.from('profiles').update({telegram_user_id:r.telegram_user_id,updated_at:new Date().toISOString()}).eq('id',p.id);await admin.from('telegram_login_requests').update({status:'verified',verified_at:new Date().toISOString()}).eq('id',r.id);return{ok:true,access_token:login.session.access_token,refresh_token:login.session.refresh_token,userId:user.id}}
Deno.serve(async req=>{try{if(req.method==='OPTIONS')return new Response('ok',{status:200,headers:cors});const url=new URL(req.url);if(url.searchParams.get('webhook')==='1')return await webhook(req);if(req.method!=='POST')return json({ok:false,error:'Method not allowed'},405);const b=await req.json();if(b.action==='start')return json(await start(b.phone,b.mode||'login',b.full_name,b.role));if(b.action==='verify')return json(await verify(b.request_token,b.code));return json({ok:false,error:'إجراء غير معروف'},400)}catch(e){console.error(e);const m=e instanceof Error?e.message:String(e);const s=m===LINKED||/مرتبط بحساب آخر|هذا الرقم مرتبط/.test(m)?400:(/غير صحيح|غير موجود|انتهت|تجاوز|يجب أن|افتح Telegram|مطلوب|نوع الحساب/.test(m)?400:500);return json({ok:false,error:m},s)}});